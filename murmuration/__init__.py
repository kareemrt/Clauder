"""murmuration: a from-scratch boids flocking simulation.

Implements Craig Reynolds' classic separation / alignment / cohesion
rules with optional predator-evasion, plus pure-stdlib renderers for
the terminal (live ASCII) and SVG (static snapshots / motion trails).
"""

__version__ = "0.1.0"
