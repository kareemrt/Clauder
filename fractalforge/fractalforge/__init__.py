"""FractalForge: a zero-dependency fractal renderer.

Computes escape-time fractals (Mandelbrot, Julia, Burning Ship, Tricorn)
and writes them straight to PNG using a hand-rolled encoder, so the only
import needed beyond the standard library is `zlib`.
"""

__version__ = "0.1.0"
